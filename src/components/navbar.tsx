import { FC } from "react";

const Navbar: FC = () => {
  return (
    <div className="bg-accent flex items-center justify-center py-4">
      <p className="text-2xl font-bold">35 佛</p>
    </div>
  );
};

export default Navbar;
